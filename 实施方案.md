# Questwright — 实施方案

> 配套文档:[设计草案.md](设计草案.md)
> 本方案把设计草案拆成可独立验收的工程任务,按依赖排序。每个阶段独立可演示,失败可回退。

---

## 0. 方案总览

### 0.1 与设计草案的差异

设计草案的 Phase 1(RAG Layer)假设全部 4 类资料都走 Docling 解析 PDF。本方案根据补充把 RAG 数据源拆成两路:

| 资料类别 | 来源 | 处理路径 |
| --- | --- | --- |
| 玩家手册 / 城主指南 / 怪物手册 (SRD) | [5e-bits/5e-database](https://github.com/5e-bits/5e-database) 仓库内的 `src/2014/en/*.json`(25 个文件) | git clone 到本地 → 直接读 JSON → 字段映射 → chunk → embed |
| 世界观文档 (world bible) | 用户上传 | Docling 解析 → heading-aware chunk → embed |

收益:
- SRD 部分无需 PDF 解析、heading 提取、CR/法术属性人工标注
- chunk metadata 天生结构化、可信
- 无需 docker / MongoDB / REST 服务,纯文件读取,网络只用于一次性 `git clone`
- 用户自带世界观仍然走原设计

### 0.2 阶段划分与里程碑

```text
Phase 0  骨架        — 项目能启动,数据库能写,API 能调
Phase 1a SRD 接入    — 5e-database JSON → Chroma,可检索 monster/spell/rule
Phase 1b 世界观接入  — 用户上传 → Chroma,可检索 lore
Phase 1c 混合检索    — Vector + BM25 + metadata filter,/retrieve API
Phase 2  最小闭环    — Campaign Brief 的 Generate / Review / Audit / Commit
Phase 3  逐层展开    — Outline / Act / Scene / Final
Phase 4  质量与成本  — LLM 日志、JSON repair、artifact diff
Phase 5  结构化 lore — 抽取 canon facts(可选)
```

每个阶段交付后用户可手动验收。验收通过才进入下一阶段。

### 0.3 技术栈锁定

```text
后端:        FastAPI + Python 3.11+
前端:        Next.js 14 (App Router) + React + Tailwind
工作流:      LangGraph (Python)
RAG:         LlamaIndex + Chroma (PersistentClient)
解析:        Docling (仅世界观)
LLM:         OpenAI (故事) + DeepSeek (审计)
DB:          SQLite (MVP) → PostgreSQL (生产)
SRD 数据:    `git clone https://github.com/5e-bits/5e-database` → 读取 `src/2014/en/*.json`
```

---

## Phase 0 — 骨架(Skeleton)

**目标**:项目能跑起来。任何阶段开发都依赖这层先稳定。

### 任务清单

| # | 任务 | 验收标准 |
| --- | --- | --- |
| 0.1 | 仓库初始化:目录结构按草案 §13;`pnpm-workspace.yaml` 注册 `apps/*` 与 `packages/*`;Python 侧用 uv 单一 workspace,根 `pyproject.toml` 把 `apps/api` 与 `services/*` 列为成员 | `pnpm install` 与 `uv sync` 都一键成功;`pnpm -r exec node -v`、`uv run python -V` 都能跑 |
| 0.2 | FastAPI 启动,健康检查 endpoint | `GET /healthz` 返回 200 |
| 0.3 | Next.js 启动,空首页 | `pnpm dev` 启动后可访问 |
| 0.4 | SQLite 初始化 + Alembic migration(7 张表见草案 §10) | `alembic upgrade head` 成功,所有表存在 |
| 0.5 | Provider Adapter 抽象层(草案 §14) | `OpenAIProvider` 和 `DeepSeekProvider` 都实现 `LLMProvider.generate()`,有单元测试 mock |
| 0.6 | Embedding Provider | 调用一次 `text-embedding-3-small` 返回向量 |
| 0.7 | LangGraph 最小 graph(只有 generate → END,无审计无人工) | 跑通一次,checkpoint 写入 SQLite |
| 0.8 | Artifact CRUD API(`POST/GET /artifacts`) | 能写入、能版本叠加(version+1, parent_id 串接) |
| 0.9 | `.env` 加载 + secrets 管理 | `OPENAI_API_KEY` / `DEEPSEEK_API_KEY` / `CHROMA_PATH` 等环境变量可注入 |

### 关键决策点(Phase 0 必须先定)

- **本地 Chroma 还是远程**:MVP 用 `PersistentClient` 本地存盘,路径写到 `.env`
- **LangGraph checkpointer**:用 `SqliteSaver`,与业务 DB 同库不同表,便于备份
- **embedding 模型**:`text-embedding-3-small`(便宜、维度 1536,后续切换成本低)

---

## Phase 1a — SRD 通过本地 JSON 文件接入

**目标**:把 SRD 数据(25 个 JSON 文件,涵盖 monsters / spells / classes / races / equipment / conditions / features / traits / magic-schools / magic-items / feats / backgrounds / rules / rule-sections / levels / proficiencies / ability-scores / skills / languages / damage-types / weapon-properties / subclasses / subraces / alignments / equipment-categories)灌进 Chroma。

### 1a.1 获取 SRD 数据

**不走 docker、不起 MongoDB、不跑 5e-srd-api REST 服务**。直接克隆 [5e-bits/5e-database](https://github.com/5e-bits/5e-database),里面的 `src/2014/en/` 目录已经是完整且整齐的 JSON 文件:

```text
5e-SRD-Ability-Scores.json   5e-SRD-Magic-Items.json
5e-SRD-Alignments.json       5e-SRD-Magic-Schools.json
5e-SRD-Backgrounds.json      5e-SRD-Monsters.json
5e-SRD-Classes.json          5e-SRD-Proficiencies.json
5e-SRD-Conditions.json       5e-SRD-Races.json
5e-SRD-Damage-Types.json     5e-SRD-Rule-Sections.json
5e-SRD-Equipment-Categories  5e-SRD-Rules.json
5e-SRD-Equipment.json        5e-SRD-Skills.json
5e-SRD-Feats.json            5e-SRD-Spells.json
5e-SRD-Features.json         5e-SRD-Subclasses.json
5e-SRD-Languages.json        5e-SRD-Subraces.json
5e-SRD-Levels.json           5e-SRD-Traits.json
                             5e-SRD-Weapon-Properties.json
```

任务:

- 在项目根用 git submodule 或独立目录管理 5e-database:

  ```bash
  git clone --depth 1 https://github.com/5e-bits/5e-database.git vendor/5e-database
  ```

  推荐 `vendor/` 目录,标记为只读外部数据,不进主仓库 git 历史(`.gitignore` 排除,或当 submodule)。
- 把路径 `vendor/5e-database/src/2014/en/` 写到 `.env` 的 `SRD_DATA_PATH`
- 仓库根 `Makefile`(或 `justfile`)加一条 `make srd-update`,执行 `git -C vendor/5e-database pull --depth 1` 便于后续同步
- **验收**:`ls $SRD_DATA_PATH/5e-SRD-Monsters.json` 存在且文件大于 1MB

> 如果网络拉 GitHub 也不稳:可在网络好的时候一次性 `git clone --depth 1` 后打包成 `5e-database-2014.tar.gz` 离线带走,后续直接解压即可。

### 1a.2 SRD JSON 加载与归一化

每个 JSON 文件是一个数组,元素结构由 5e-database 维护(与 5e-srd-api REST 返回的字段一致)。统一映射成 `RawSrdRecord`:

```python
class RawSrdRecord(BaseModel):
    source: Literal["monster_manual", "phb", "dmg", "srd_misc"]
    file_name: str           # 例如 "5e-SRD-Monsters.json"
    category: str            # 例如 "monsters", "spells"
    index: str               # JSON 中的 `index` 字段,例如 "young-red-dragon"
    name: str                # JSON 中的 `name` 字段
    raw_json: dict           # JSON 数组中的单条记录
    loaded_at: datetime
```

source 映射规则(同原方案):

- `5e-SRD-Monsters.json` → `monster_manual`
- `5e-SRD-Spells.json` / `Classes` / `Subclasses` / `Races` / `Subraces` / `Features` / `Traits` / `Ability-Scores` / `Skills` / `Proficiencies` / `Languages` / `Backgrounds` / `Feats` / `Levels` → `phb`
- `5e-SRD-Rules.json` / `Rule-Sections.json` → `dmg`(SRD 中的规则段落,语义上偏 DM 参考)
- 其余(`Magic-Schools` / `Damage-Types` / `Conditions` / `Equipment` / `Equipment-Categories` / `Weapon-Properties` / `Magic-Items` / `Alignments`)→ `srd_misc`

任务:

- 实现 `services/rag/srd_loader.py`,扫描 `SRD_DATA_PATH` 目录,逐文件 `json.load` 后逐条 yield `RawSrdRecord`
- 内置 `FILE_TO_SOURCE` 字典,新增文件时只改这一处
- CLI:`python -m services.rag.cli load-srd --dry-run` 打印每个文件的记录数
- **验收**:`load-srd --dry-run` 输出与 `cat src/2014/en/*.json | jq length` 一致

### 1a.3 SRD → Chunk 映射

每个 category 一个 `Chunker`,产出符合草案 §5.3 的 chunk:

```python
class RagChunk(BaseModel):
    id: str                       # 例如 "srd_monster_young-red-dragon"
    source: str                   # 见上
    source_title: str             # "Monster Manual" 等
    edition: Literal["5e"] = "5e"
    chunk_type: str               # "monster_statblock", "spell_chunk", ...
    title: str                    # 显示名
    section_path: list[str]       # ["Dragons", "Red Dragon"] 等
    tags: list[str]               # 提取自 raw_json (CR、damage type、school...)
    text: str                     # 给 embedding 的合成文本
    metadata: dict                # 5e-database 关键字段(CR、HP、AC、spell level...)
    canon_level: Literal["hard", "soft", "advisory"] = "hard"  # SRD 默认 hard
    source_ref: str               # 例如 "5e-SRD-Monsters.json#young-red-dragon" 便于追溯
```

**文本合成模板**(关键,影响检索质量):
- monster:`{name} (CR {cr}, {size} {type}). AC {ac}, HP {hp}. Actions: {actions_text}. Special: {special_abilities_text}. ...`
- spell:`{name} — Level {level} {school}. Range {range}. Components {components}. {desc}. Higher levels: {higher_level}`
- class/race/feature:简单展开 `desc[]`
- rule / rule-section:直接拼 `name` + `desc`

任务:

- `services/rag/chunkers/monster_chunker.py` 等(每个 category 一个文件,通过 registry 注册)
- 单元测试:每个 chunker 至少 3 个 fixture(典型、边界、空字段)
- **验收**:所有 25 个 JSON 文件可以无错误转换成 `RagChunk` 列表,总数可统计

### 1a.4 Embedding 与 Chroma 入库

任务:
- `services/rag/embed.py`:批量调 embedding(默认 64 条/batch)
- `services/rag/index.py`:写入 Chroma,collection 名 `dnd_rag`
- Chroma metadata 只存可过滤字段(`source`, `chunk_type`, `tags`, `cr`, `spell_level`)。完整 metadata 落 SQLite `rag_chunks` 表
- 幂等:`chunk_id` 已存在则更新 embedding 而非新增
- CLI:`python -m services.rag.cli reindex-srd`
- 验收:Chroma collection count 等于 SRD chunk 总数;SQL `rag_chunks` 表一致

### 1a.5 验收(Phase 1a Demo)

写一个临时 CLI 脚本演示:
```bash
python -m services.rag.cli search "fire breathing dragon CR 10"
# 返回 top 5,第一条应为 Young Red Dragon
```

---

## Phase 1b — 世界观文档接入

**目标**:用户上传一份 Markdown / PDF 世界观,系统切 chunk 入 Chroma。

### 任务清单

| # | 任务 | 验收标准 |
| --- | --- | --- |
| 1b.1 | `POST /projects/{id}/sources` 上传文件(multipart) | 文件落到 `data/uploads/{project_id}/`,DB 记录 source |
| 1b.2 | Docling 解析为 Markdown | 输出 `.md` 文件落盘到 `data/parsed/` |
| 1b.3 | Heading-aware chunker | 按 `#` / `##` / `###` 切分,保留 `section_path`,chunk 大小目标 500-1000 token |
| 1b.4 | 必填 metadata:`canon_level`(默认 `soft`,可手动改)、`tags`(空)、`chunk_type=lore_chunk` | DB 落库 |
| 1b.5 | Embedding + Chroma 入库,同 1a.4 | Chroma count 增加 |
| 1b.6 | `POST /projects/{id}/sources/{source_id}/ingest` 触发整个流程 | 异步返回 task_id,前端轮询状态 |

### 决策点
- **PDF 优先级**:MVP 只支持 Markdown,PDF 留到 Phase 1b.2(Docling 支持但调试成本高)
- **chunk 重叠**:默认 0 重叠,简化版本管理。如检索召回不足再加 100 token 重叠

---

## Phase 1c — 混合检索 API

**目标**:`POST /projects/{id}/retrieve` 返回符合草案 §11.2 的 chunks。

### 任务清单

| # | 任务 | 验收标准 |
| --- | --- | --- |
| 1c.1 | Vector retriever(Chroma top 20) | 单测通过 |
| 1c.2 | BM25 retriever(本地 rank_bm25,索引存 SQLite 或内存) | 单测通过 |
| 1c.3 | Metadata filter(`source`, `chunk_type`, `cr_range`, `tags`) | 支持复合过滤 |
| 1c.4 | Merge + dedupe(按 `chunk_id`)+ 加权(参考草案 §5.4) | top-k 可调,默认 8 |
| 1c.5 | 可选 reranker(MVP 跳过,接口预留) | 接口签名稳定 |
| 1c.6 | `POST /retrieve` API | 返回 schema 符合草案 §11.2 |
| 1c.7 | 前端 "Knowledge Page":输入 query,显示命中 chunks(草案 §12.1) | 可调试看 metadata |

### 关键决策
- BM25 索引存储:用 `rank_bm25` Python 库,启动时从 SQLite 加载所有 chunk 的 text 构建内存索引。chunk 增删时增量重建。规模 < 10k chunk 完全够用
- 权重配置:`config/retrieval.yaml`,按 stage 切换 source 权重(故事生成 vs 审计)

---

## Phase 2 — 最小闭环(Campaign Brief)

**目标**:用户输入 brief 需求,系统跑通 "生成 → 人工 → 审计 → 人工 → 落库",一个 stage 走完。

### 2.1 Prompt 与 Schema

- `prompts/story_generator/campaign_brief.md` — 草案 §6.1 prompt 模版,包含 retrieved_context 占位、user_brief 占位、committed_artifacts 占位、constraints 占位
- `packages/shared-schemas/campaign_brief.schema.json` — 输出 JSON Schema(title/pitch/theme/main_conflict/main_villain/acts/canon_references)
- `prompts/auditor/world_audit.md` — 草案 §6.2 prompt
- `packages/shared-schemas/audit_report.schema.json`

### 2.2 LangGraph 图

节点:
```
build_context → generate_artifact → wait_for_human_review → [revise_artifact 或 audit_artifact]
audit_artifact → wait_for_audit_review → [revise_artifact 或 commit_artifact]
commit_artifact → END (Phase 2 暂停)
```

实现要点:
- `wait_for_*` 节点使用 LangGraph `interrupt`,checkpoint 持久化
- `POST /module-runs/{run_id}/resume` 接收人工决策(`approve` / `request_changes` / `reject` / `return_previous_stage`),恢复 graph
- 每次 LLM 调用前后写 `llm_call_logs`

### 2.3 任务清单

| # | 任务 | 验收标准 |
| --- | --- | --- |
| 2.1 | `POST /projects/{id}/module-runs` 启动一个新 run | 返回 run_id,DB workflow_runs 记录 |
| 2.2 | `build_context` 节点:用 user_brief 调 retrieve,top 8 | retrieved_chunk_ids 写到 state |
| 2.3 | `generate_artifact` 节点:调 OpenAI,JSON Schema 校验 | artifact 落库,status=`pending_human_review` |
| 2.4 | LangGraph 中断,等待 review | run.status=`waiting_human_review` |
| 2.5 | `POST /artifacts/{id}/review`(草案 §11.4) | 写 human_reviews,resume graph |
| 2.6 | `revise_artifact` 节点(approve 路径不走,request_changes 走) | fork 新 artifact, parent_id 串接 |
| 2.7 | `audit_artifact` 节点:DeepSeek + audit prompt | audit_reports 落库 |
| 2.8 | 第二次中断,等待 audit review | run.status=`waiting_audit_review` |
| 2.9 | `POST /audit-reports/{id}/review` | resume graph |
| 2.10 | `commit_artifact` 节点 | artifact.status=`committed`,artifact_commits 写记录 |
| 2.11 | 前端 "Workflow Run Page"(草案 §12.1) | 显示当前 stage / artifact / audit / 4 个按钮 |
| 2.12 | 前端 "Artifact Compare Page"(v1 vs v2 diff) | 看到修订前后差异 |

### 2.4 验收
完整跑通一次:
1. 上传世界观 + 加载 SRD
2. 创建项目,输入 user_brief("一个 5 级队伍的 3-session 哥特恐怖模组")
3. 系统生成 brief,显示给用户
4. 用户点 "Request changes",填反馈
5. 系统 fork v2,再展示
6. 用户 "Approve",触发审计
7. 审计返回 issues(可能 0 issues)
8. 用户审 audit,"Approve"
9. brief committed,run 状态变为 `done`(Phase 2 只跑一个 stage)

---

## Phase 3 — 多阶段逐层展开

**目标**:把 Phase 2 的 stage 串成 5 层(brief → outline → act → scene → final)。

### 任务清单

| # | 任务 | 验收标准 |
| --- | --- | --- |
| 3.1 | 每个 stage 一份 prompt + schema | 5 套都有 |
| 3.2 | `advance_stage` 节点:把上一个 committed artifact 注入下一个 stage 的 context | state.committed_artifact_ids 增长 |
| 3.3 | Stage 间依赖:`act_outline` 引用 `campaign_outline` 的 act_id 等 | schema 强制 foreign key 字段 |
| 3.4 | `Return to Previous Stage` 按钮:从上一个 committed artifact fork 新分支 | 旧 stage 的 committed 仍保留 |
| 3.5 | 同一 stage 可生成多个并行 artifact(例如 4 个 act 各自一个 act_outline) | state.next_action 能跨 artifact 分发 |
| 3.6 | 前端进度条 + stage 切换 UI | 用户能看出当前在哪一层 |

### 决策点
- **act_outline 是 1 个 artifact 还是 N 个(每幕一个)?** — 建议 N 个,便于按幕重试。schema 里 `act_id` 关联 campaign_outline
- **scene_draft 同理**,按 scene 拆开
- **final_module** 是一个聚合 artifact,把所有 committed 内容拼成 DM 可读文本(纯模板,不调 LLM,或仅一次润色)

---

## Phase 4 — 质量与成本

**目标**:系统可用、可观测、可控本。

### 任务清单

| # | 任务 | 验收标准 |
| --- | --- | --- |
| 4.1 | `llm_call_logs` 表全量写入,每次 LLM 调用记录 model / tokens / cost / latency | DB 可查 |
| 4.2 | Prompt 版本化:`prompts/**` 文件每次提交记录 `prompt_hash`,artifact 关联 | 回溯到当时的 prompt |
| 4.3 | JSON parser 失败自动 repair(再调一次 LLM 修复),失败重试 1 次,仍失败 workflow 标 `failed` | 单测覆盖 |
| 4.4 | Artifact diff API + 前端 diff 视图 | 可看每次修订的字段级变化 |
| 4.5 | Cost dashboard(简单聚合 `llm_call_logs`) | 按 run / project 看花费 |
| 4.6 | 按 stage 配置模型:brief/final 用 GPT 高档,outline/act 用 GPT 中档,scene 可用 mini | `config/models.yaml` |

---

## Phase 5 — 结构化 Canon Facts(可选,看 Phase 2-4 审计效果)

**触发条件**:Phase 2-4 跑下来发现 World Auditor 漏检率过高(例如世界观冲突没抓到)。

### 任务清单(摘自草案 §17 Phase 5)

| # | 任务 | 验收标准 |
| --- | --- | --- |
| 5.1 | 从 world_bible chunks 抽取 canon facts(轻量 LLM,DeepSeek) | facts 表落库 |
| 5.2 | 标 hard / soft constraint | 人工 review UI |
| 5.3 | NPC / faction / location entity index | 简单 KV 表 |
| 5.4 | facts-based auditor:在 audit prompt 中显式列举相关 hard facts | 审计准确率提升 |

---

## 跨阶段的工程规范

### 测试策略
- **单元测试**:chunker、provider adapter、schema validation 必须覆盖
- **集成测试**:每个 phase 至少 1 个端到端测试,模拟人工 review
- **LLM 测试**:用 `recorded_responses` fixture,避免每次跑测试都烧 API

### 安全 / 边界
- 上传文件大小限制(默认 20MB)
- 用户 prompt 不能注入到 system prompt(用 markdown 包裹 + 明确分隔)
- LLM 调用全部异步,带 timeout(默认 60s)和重试

### 风险与对策(从草案 §18 提取)
| 风险 | 阶段 | 对策 |
| --- | --- | --- |
| RAG 召回不准 | 1c | hybrid + metadata filter + 检索 debug UI |
| LLM JSON 不稳 | 2 | schema validation + repair + retry |
| 版本混乱 | 2 | 永不原地改,fork + parent_id |
| LangGraph state 与业务 DB 混淆 | 2 | checkpoint 只存运行态,artifact 走业务表 |
| 成本失控 | 4 | 全量日志 + 分层模型 + 强制走 RAG context |

---

## 当前建议的下一步

已确认事项:

- **SRD 接入形态**:`git clone --depth 1 https://github.com/5e-bits/5e-database.git vendor/5e-database`,直接读 `src/2014/en/*.json`,不走 docker 不起 MongoDB
- **LLM provider 与 key**:由用户在 `.env` 手动统一配置;代码层面只留环境变量占位
- **monorepo 工具**:**pnpm**(JS/TS 侧)+ **uv**(Python 侧)

可以从 Phase 0.1(仓库初始化)开始按任务清单逐项实施。
